{
  "name": "Archive Binder",
  "install": "npm install",
  "terminals": [
    {
      "name": "api",
      "command": "npm run dev:server",
      "description": "Express + TypeScript API (GATCG proxy + collection) on http://localhost:3001"
    },
    {
      "name": "web",
      "command": "npm run dev:client",
      "description": "Vite + React mobile scanner UI on http://localhost:5173 (proxies /api to the API terminal)"
    }
  ],
  "ports": [
    { "name": "api", "port": 3001 },
    { "name": "web", "port": 5173 }
  ]
}
